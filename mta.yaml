_schema-version: "3.2"
ID: mnmnapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: mnmnapp-destination-content
  type: com.sap.application.content
  requires:
  - name: mnmnapp-destination-service
    parameters:
      content-target: true
  - name: mnmnapp-repo-host
    parameters:
      service-key:
        name: mnmnapp-repo-host-key
  - name: mnmnapp-uaa
    parameters:
      service-key:
        name: mnmnapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: mnmnapp_html_repo_host
          ServiceInstanceName: mnmnapp-html5-service
          ServiceKeyName: mnmnapp-repo-host-key
          sap.cloud.service: mnmnapp
        - Authentication: OAuth2UserTokenExchange
          Name: mnmnapp_uaa
          ServiceInstanceName: mnmnapp-xsuaa-service
          ServiceKeyName: mnmnapp-uaa-key
          sap.cloud.service: mnmnapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: mnmnapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: mnmnapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - mnmnapp.zip
      name: mnmnapp
      target-path: resources/
- name: mnmnapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: mnmnapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: mnmnapp-destination-service
    service-plan: lite
- name: mnmnapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: mnmnapp-xsuaa-service
    service-plan: application
- name: mnmnapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: mnmnapp-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
